(ns campaign2.core
  (:require [campaign2
             [relic :as relic]
             [state :as state]])
  (:import (java.util HashMap)))


(defn start []
  (HashMap.))

(defn -main [& _]
  (start))
